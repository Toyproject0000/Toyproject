import 'package:flutter/cupertino.dart';

class messageTime {
  final DateTime date;
  messageTime({required this.date});

  Row chattingDay(){
    return Row(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [
        // Text();
      ],
    );
  }
}